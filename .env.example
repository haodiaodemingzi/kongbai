# 域名配置
DOMAIN=example.com
EMAIL=admin@example.com

# MySQL 配置
MYSQL_ROOT_PASSWORD=your_root_password
MYSQL_DATABASE=kongbai
MYSQL_USER=kongbai_user
MYSQL_PASSWORD=your_password

# 应用配置
FLASK_ENV=production
FLASK_APP=app
SECRET_KEY=your_secret_key

# 服务器配置
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# 数据库配置
# 使用PyMySQL连接
# 示例1（使用传统认证，不需要cryptography包）:
# DATABASE_URL=mysql+pymysql://用户名:密码@数据库地址/数据库名?charset=utf8mb4&auth_plugin=mysql_native_password
# 示例2（使用SHA2认证，需要安装cryptography包）:
# DATABASE_URL=mysql+pymysql://用户名:密码@数据库地址/数据库名?charset=utf8mb4
DATABASE_URL=mysql+pymysql://root:password@localhost/oneapi?charset=utf8mb4

# 日志配置
LOG_LEVEL=DEBUG  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# 时区配置
TZ=Asia/Shanghai 